# AGENTS.md

## 协作

- 默认使用中文、PowerShell 和 UTF-8；本机环境为 Windows 11、PowerShell 7+，以实际执行环境为准。
- 按任务需要了解相关代码和约定，自主完成实现、必要验证与修正；仅在缺失信息会实质改变方向或风险时提问，普通可回退步骤不反复确认。
- 当前明确需求优先于历史约定和技能建议；发现冲突时按适用范围解决，无法判断且影响结果时再询问。

## 工程

- 在当前目标和约束下选择综合收益最佳、能够验证的方案，兼顾正确性、健壮性、层级、边界、职责、命名、可读性和维护成本；不以最小 diff 为目标，也不扩大到无关重构。
- 优先修复根因；必要时调整相关模块及直接调用链。抽象、依赖和框架须有实际收益，不为未知需求预建结构。
- 替换方案时直接收敛到当前实现，不增加历史兼容层；同步清理被替代的实现、入口、命名、文档、基线和仅服务于旧方案的测试，保留仍验证当前需求的回归测试。
- 代码、文件和文档命名表达业务含义与职责，不使用版本号或开发过程、阶段标识；外部协议、依赖版本、必要迁移编号和真实业务概念除外。
- 在真实输入、系统与信任边界进行必要校验和错误处理；不增加无依据的兜底、重复校验或掩盖错误的静默降级。

## 验证与审查

- 按改动风险运行直接相关的有效测试和必要检查；新增测试应验证当前行为、关键边界或真实缺陷，不机械镜像实现，不以无需求的快照或哈希断言固化实现细节。
- 相关检查通过后结束验证；仅在新改动、失败或未解决风险出现时扩大或重复检查。交付前检查最终改动，避免遗留无关内容。
- 仅审查时不修改代码。只报告有依据、值得处理的问题，按严重程度说明位置、触发条件、影响和修复方向；没有明确问题就直说。

## 边界与交付

- 保留用户已有修改，不覆盖或删除无关内容，不泄露敏感信息；未经授权不提交、推送、切换分支或执行破坏性操作。
- 结论先行，简洁说明结果、关键取舍、实际验证和未解决问题；不编造事实或验证结果，未验证时明确说明。
- 仓库根目录存在 `.codegraph/` 且需要理解或定位代码时，优先使用 CodeGraph；不可用时使用常规检索，不自行建立索引。

## 项目补充约束

- 论文资产：`thesis-assets/meta/workflow-governance.md` 为论文工作流最高规范，与其他规范冲突时以其为准。正文唯一真相源为 `thesis-assets/chapters/*.md`；证据未锁定严禁重写正文；PaperSpine 严格单章执行；阶段推进须用户与审查官复核，本地不自行判定通过。
- 界面规范：`DESIGN.md` 为视觉与交互最高规范。UI 改动验证命令以 `docs/setup.md#验证` 与 `docs/quality/ui-quality-system.md` 为准。
- GitHub 内容：PR 正文只描述最终交付、必要架构与稳定契约，禁止写入 Issue 编号或链接，禁止 `Closes`、`Fixes`、`Resolves`、`Refs` 等关联标识；Issue 关系只通过 GitHub Development 原生关系维护；验证事实只由 GitHub Checks / Actions 表达。
