# 业务与性能采集测试记录（dev fixture / 历史真实 API）

**测试日期**：2026-06-12
**测试环境**：本机 Windows 11 全栈环境
**测试模式**：dev fixture 本地验收数据夹具 / 历史真实 API 对照

## 文件定位

本文对应毕业论文第五章 dev fixture、本地验收数据夹具、权限与数据隔离以及未实测性能边界的补充证据。当前系统已收敛为 `start-dev.bat` 与 `start-docker.bat`；旧 Demo Twin 只作为历史对照，不代表当前默认运行模式。

## 1. 核心业务性能指标

| 性能指标项 | 采集状态 | 实测耗时 | 备注 |
| --- | --- | --- | --- |
| PDF 上传/解析接口耗时 | 未完成当前批量实测 | N/A | dev fixture 主要验证本地流程可达，不统计复杂多页 PDF 成功率 |
| SSE 首字节响应时间 | 未完成当前网络实测 | N/A | 真实网络延迟与 LLM Provider token 流式耗时待独立测试 |
| LLM 响应与语音合成耗时 | 未完成真实端到端实测 | N/A | TTS 目前有顺序、失败、timeout 单元测试，不代表真实服务低延迟基准 |
| dev fixture 数据重置与本地验收机制 | 已通过 | < 100ms | 本地验收数据可重置，不进入 Full Docker / prod 默认路径 |
| Lua 脚本高频限流触发 | 未完成压测 | N/A | 仅可描述实现机制，不可写成压测通过 |

## 2. 历史真实模型调用记录

`real-llm-api-2026-05-27-redacted.md` 与 `.json` 保留为历史真实 API 功能链路记录。该数据产生于 5 月底，与 6 月最新框架和质量门禁存在时间差，只作为补充对照，不作为当前默认 Provider、默认模型或性能基准依据。

## 3. 写作边界

- 可写：dev fixture 提供本地可重置验收数据，适合验证页面可达、基本业务闭环和自动化脚本稳定性。
- 可写：历史真实 API 记录证明系统曾完成真实模型功能链路，但具体时延只代表当时网络和模型条件。
- 不可写：高并发压测已完成。
- 不可写：真实 ASR/TTS 低延迟指标已通过。
- 不可写：Resilience4j、Redis 限流在极端拥堵下已有量化压测结论。
