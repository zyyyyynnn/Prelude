# 阶段 2：证据与资产同步审查笔记

> **口径说明（2026-06 补记）**：本笔记中提到的 RabbitMQ 作为"缺失组件"，反映的是当时的审查判断。项目已确认将引入 RabbitMQ 作为报告生成异步任务队列的后续升级方向。当前代码层面仍采用 Redis List 实现报告生成任务的轻量级异步入队与消费，后续将以 RabbitMQ 替换。RabbitMQ 在补齐 AMQP 依赖、Docker Compose 服务、生产者/消费者代码、测试记录和证据资产之前，不得写成已实现能力。

## 图 3.3 系统架构图影响核对

**图 3.3**：阶段 3 前需重画

**原因**：
图 3.3 当前仅涵盖了 Vue、Spring Boot、MySQL、PDFBox、LLM Provider、Demo Twin 以及 SSE 流式返回的宏观节点。然而，根据 2026 年 6 月的最新工程实现基线，系统架构存在显著扩容：
1. **缺失组件**：Redis（用于限流与会话状态）、RabbitMQ、Prometheus / Grafana（监控矩阵）、Resilience4j（熔断降级组件）。
2. **缺失链路**：未标明 WebSocket 实时语音拾音的上下行双向链路，目前仅有 SSE 的单向返回。
3. **节点精度**：LLM Provider 当前已支持多供应商配置切换，而不仅仅是单一节点。

基于上述遗漏点，为了保证第三章系统设计与第四章系统实现的紧密自洽，必须在阶段 3（正文与图表重绘阶段）中，使用 `nature-figure` 对图 3.3 进行重画与结构扩充。

## 历史证据 MIMO 2026-05-27 处理策略

**策略**：暂不直接写入第五章的正式测试数据表格中，但作为补充证据纳入锁定范围。
**原因**：该测试数据时间戳为 5 月底，存在版本时效性差异。可供答辩对照使用或正文文本引用论证 TTFB。
