# Backend
backend/target/
backend/uploads/

# Docker Compose local env (do not commit real secrets)
.env

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.env*
!frontend/.env.example
frontend/.env.local

# Playwright
.playwright-cli/

# Logs
*.log

# Generated run artifacts. The tracked interface assets live in
# docs/screenshots/surfaces/ and are regenerated by `npm run capture:surfaces`.
output/

# Office temporary lock files
~$*.docx
~$*.xlsx
~$*.pptx
*.tmp

# Local review bundles and one-off backups
thesis_literature_enhancement_package.zip
thesis-assets/*.before-literature.*

# Eclipse project metadata
backend/.classpath
backend/.factorypath
backend/.project
backend/.settings/

# Workspace-local Playwright browser cache. Mirrors the
# `PLAYWRIGHT_BROWSERS_PATH=${{ github.workspace }}/.cache/ms-playwright`
# setting in .github/workflows/ci.yml — local devs who copy that env var
# for parity with CI get a gitignored, ephemeral cache directory.
.cache/
backend/uploads/
backend/startup*.log

target/
build/
